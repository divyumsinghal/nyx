[build]
# Mold linker for fast builds (disabled - not available in this environment)
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

[alias]
xtask = "run --package nyx-xtask --"
