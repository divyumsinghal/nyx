# Tools — Development Utilities

> Seed data and development helper scripts.

```
tools/
└── seed-data/           # JSON seed data for local development
    ├── users.json
    └── uzume_posts.json
```

## Purpose

- `seed-data/` contains JSON files for seeding the database during local development
- Used by `nyx-xtask` seed command or manual DB insertion
- Add more seed files here as needed for testing
